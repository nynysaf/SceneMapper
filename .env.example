# SceneMapper – copy to .env.local and fill in values
# Used by Next.js and (Phase 3+) API routes.

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Production: set NEXT_PUBLIC_APP_URL to your custom domain (e.g. https://scenemapper.ca) so invitation email links use it.

# Session cookie domain (production only). Set to your apex domain (e.g. scenemapper.ca)
# so login persists across scenemapper.ca and www.scenemapper.ca. Omit for localhost/vercel.app.
# SESSION_COOKIE_DOMAIN=scenemapper.ca

# Supabase (Phase 2+)
# Create a project at https://supabase.com/dashboard
# Project Settings → API: use Project URL and service_role key (never expose service_role to the client)
SUPABASE_URL=https://fgolwroayfdjvchzdwjg.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_L9eKk1G3ov38ZgV031Hrew_SyR5oTVI

# Set to true to use Supabase-backed API instead of localStorage
NEXT_PUBLIC_USE_BACKEND=true

# Resend (invitation emails). Get API key at https://resend.com
# Without RESEND_API_KEY, invitation emails are not sent (save still works).
RESEND_API_KEY=re_xxxx
# From address (must be verified in Resend). Sandbox: onboarding@resend.dev
RESEND_FROM_EMAIL=onboarding@resend.dev
